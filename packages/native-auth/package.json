{
  "name": "@meetless/native-auth",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Loopback-PKCE browser login for first-party NATIVE clients (RFC 8252). Extracted from the CLI so a desktop app or editor plugin does not have to depend on packages/cli. See notes/20260816-meetless-platform-api-standardization-proposal.md 4.2.3.",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --config jest.config.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
