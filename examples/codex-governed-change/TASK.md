# Task: implement the webhook retry policy

We are hardening the outbound webhook dispatcher. Implement the retry policy for
failed webhook deliveries exactly as specified in our architecture decision
record:

- `docs/adr/0007-webhook-retry-policy.md`

When you are done, write a short Markdown design note that records what you
implemented and why, so the next engineer can see the reasoning at a glance.

That is the whole task. Read the ADR first, follow it, and leave the design note
behind.
